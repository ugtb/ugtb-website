---
interface Props {
  class?: string;
  name: string;
  size?: number;
}

const { class: className, name, size = 24 } = Astro.props;
const href = `icons.svg#${name}`;
---

<svg class:list={className} width={size} height={size}
  ><use xlink:href={href}></use></svg
>
