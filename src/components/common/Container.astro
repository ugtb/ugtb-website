---
import type { HTMLAttributes } from 'astro/types';

type Padding = 'sm' | 'md' | 'lg';

interface Props extends HTMLAttributes<'div'> {
  left?: Padding;
  right?: Padding;
}

const {
  'class': className,
  'class:list': classList,
  left,
  right,
} = Astro.props;
---

<div
  class:list={[
    'max-w-[1440px] w-full mx-auto md:pl-20',
    { 'pl-6': left, 'pr-6 md:pr-20': right },
  ]}
>
  <div
    class:list={[
      {
        'md:border-l md:border-l-neutral md:pl-18':
          left === 'md' || left === 'lg',
        'lg:pl-28': left === 'lg',
        'lg:pr-10': right === 'lg',
      },
      className,
      classList,
    ]}
  >
    <slot />
  </div>
</div>
